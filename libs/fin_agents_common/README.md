# fin_agents_common

Shared library for all `fin_agents` agents: configuration, logging, LiteLLM client (API keys + Vertex service-account JSON), and OpenTelemetry/Langfuse observability.

```bash
pip install -e "libs/fin_agents_common[langfuse]"
```
